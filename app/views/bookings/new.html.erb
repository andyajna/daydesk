<div class="container">
	<div class="row">
		<div class="col-md-4">
			<h4>Send Booking to <%= @service.supplier.name %></h4>
			<h5>For their <%= @service.desktype %> service </h5>
			<%= render 'form' %>
		</div>
	</div>
</div>
